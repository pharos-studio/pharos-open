# 前端设计系统 G0 · 深藏蓝×鎏金

> 版本：随项目前端搭建落地，持续维护。
> 单一真相源：`public/style.css` 顶部 `:root`。

## 1. 设计方向
- **配色**：深藏蓝 `#0A1020` 画布 + 极光贴边（靛蓝左上 / 鎏金右上）+ 玻璃纸卡片 + 鎏金 `#D9A441` 强调。
- **字体**：标题用衬线（宋体系），正文/数字用无衬线；数字 `tabular-nums`。
- **涨跌**：A股惯例，**涨红 `#FF8F8F` / 跌绿 `#6FCF97`**。
- **动效**：状态切换 150–250ms；尊重 `prefers-reduced-motion`。（未引入数字滚动等强动效，避免与「不替用户决定」的极简基调冲突。）
- **响应式**：桌面左侧边栏 6 项；手机顶部品牌栏 + 底部 4 Tab（概览/决策/持仓/更多）。

## 2. Tokens（`:root`）
| Token | 值 | 用途 |
|---|---|---|
| `--bg` | radial-gradient(...) + `#0A1020` | 页面背景 |
| `--panel` | `rgba(255,255,255,0.05)` | 卡片/面板底色 |
| `--panel-strong` | `rgba(255,255,255,0.07)` | 悬停/强调面板 |
| `--border` | `rgba(255,255,255,0.12)` | 细线边框 |
| `--border-strong` | `rgba(255,255,255,0.20)` | 加重边框 |
| `--gold-line` | `rgba(217,164,65,0.22)` | 面板头金线 |
| `--text` | `#EDEFF7` | 主文字 |
| `--text-muted` | `#9AA3C0` | 次要文字 |
| `--accent` | `#D9A441` | 鎏金：主操作/强调 |
| `--accent-soft` | `#E8C57A` | 鎏金浅：悬停/高亮 |
| `--up` | `#FF8F8F` | 涨（红） |
| `--down` | `#6FCF97` | 跌（绿） |
| `--badge-add-bg/fg` | `rgba(217,164,65,0.16)` / `#E8C57A` | 加仓徽章 |
| `--badge-hold-bg/fg` | `rgba(154,163,192,0.14)` / `#A9B1CC` | 不动徽章 |
| `--badge-stop-bg/fg` | `rgba(255,143,143,0.14)` / `#FF8F8F` | 减仓/停止徽章 |
| `--radius` | `12px` | 卡片圆角 |
| `--radius-sm` | `7px` | 按钮/输入框圆角 |
| `--font-display` | Songti SC, STSong, Noto Serif CJK SC, Source Han Serif SC, SimSun, Georgia, serif | 标题衬线 |
| `--font-sans` | -apple-system, BlinkMacSystemFont, PingFang SC, Microsoft YaHei, Segoe UI, sans-serif | 正文无衬线 |

## 3. 文件结构（已落地）
```
public/
├── index.html                 # 壳 + 单 #view 容器 + hash 路由
├── style.css                  # 设计系统 tokens + 全局样式 + 响应式
├── manifest.webmanifest       # PWA 清单（加到主屏）
├── sw.js                      # Service Worker（离线缓存；改版需升 CACHE 版本号）
├── icon.svg                   # PWA 图标
├── vendor/
│   └── echarts.min.js         # 由 trend.js 动态 import 加载（非阻塞首屏）
└── js/
    ├── app.js                 # hash 路由 + 启动
    ├── api.js                 # /api/* 封装
    ├── store.js               # 全局缓存 + localStorage 设置
    ├── util.js                # 格式化 / DOM / 颜色类
    ├── pages/
    │   ├── overview.js        # 概览
    │   ├── decision.js        # 决策
    │   ├── holdings.js        # 持仓（入口壳，只 re-export；勿在此加实现）
    │   ├── holdings/          # 持仓页实现（13 个模块，路径约定见 §6）
    │   │   ├── index.js       #   入口：整页渲染分发 + 注册重绘回调
    │   │   ├── state.js       #   共享状态（容器/展开项/端形态）+ 刷新后重绘
    │   │   ├── constants.js   #   分类兜底、口径选项、跟踪指数提示等纯数据
    │   │   ├── fundMeta.js    #   基金名单懒加载、市场推断、分类建议、买入归集
    │   │   ├── fundStore.js   #   读基金列表、持久化、增删基金
    │   │   ├── formShell.js   #   时段开关 + 买入行容器（桌面表格行／手机卡片）
    │   │   ├── preview.js     #   买入预览：防抖、竞态丢弃、净值份额预估
    │   │   ├── purchaseForm.js#   记一笔 / 编辑记录
    │   │   ├── dailyLimit.js  #   每日限购展示与编辑
    │   │   ├── desktop.js     #   桌面整页渲染 + 展开子表
    │   │   ├── mobile.js      #   手机整页渲染 + 卡片明细
    │   │   ├── bulkAdd.js     #   批量粘贴解析与写入
    │   │   └── addFundPanel.js#   添加基金面板
    │   ├── allocation.js      # 配置（含穿透分析）
    │   ├── review.js          # 复盘
    │   ├── settings.js        # 设置
    │   └── more.js            # 手机「更多」
    └── charts/
        └── trend.js           # ECharts 走势图（动态 import echarts）
```

## 4. 页面数据契约
- `STATE = /api/state`：`holdings.funds`（数组，每项含 purchases[]）/ `target` / `config` / `history` / `watchlist`。
- `LIVE = /api/refresh`：`funds`（富化数组）/ `totals` / `allocation` / `plan` / `penetration`。
- `advice = /api/advice?session=am|pm`：`l1`（组合状态）/ `l2`（信号数组）/ `l3`（月度计划）。

## 5. 用法约定
- 改配色 → 只改 `style.css` `:root`。
- 改某页 → 只改 `js/pages/xxx.js`。持仓页已拆分，改 `js/pages/holdings/` 下对应子模块（入口壳 `js/pages/holdings.js` 只 re-export，**不要**在里面加实现）。
- 改接口/鉴权 → 只改 `js/api.js`。
- 改本地存储 → 只改 `js/store.js`。
- 新增页面 → 新建 `js/pages/xxx.js` + `app.js` 的 `PAGES` 加一行（`index.html` 无 section，路由全靠 hash，无需改壳）。

## 6. 已知坑与处理
- **模块路径约定**：`../` 的层数 = **该文件在 `public/js/` 下的目录深度**（唯一算法，别再靠数点）。顶层（`js/*.js`）互相 `./xxx.js`；`pages/`、`charts/`（深度 1）引用 `js/` 下模块用 `../xxx.js`；`pages/` 下的子目录如 `pages/holdings/`（深度 2）引用 `js/` 下模块用 `../../xxx.js`，子目录内互相 `./xxx.js`；`vendor/` 用 `../vendor/xxx.js`（深度 2 时为 `../../vendor/xxx.js`）。当前唯一二层目录是 `pages/holdings/`；新增子目录请按此公式推导。
- **holdings.funds 是数组**：前端用 `state.holdings.funds` 直接迭代（`.forEach`/`.map`），不要 `Object.values`。
- **echarts 懒加载**：`echarts.min.js` 1MB，由 `js/charts/trend.js` 动态 `import('../charts/trend.js')` 时按需加载，**不阻塞首屏**。概览页 KPI 同步、走势图异步。
- **玻璃性能**：`backdrop-filter: blur(14px)` 在低端 Android WebView 可能掉帧；若卡，改 `--panel` 为实色 `rgba(255,255,255,0.06)` 并去掉 `backdrop-filter`。
- **WCAG AA**：主文字 `#EDEFF7` on `#0A1020` ≈ 15:1；鎏金 `#D9A441` on `#0A1020` ≈ 8.4:1；均过 AA。
