# 更新日志

给使用者看的变更记录，新条目加在最上面。只记「你能感觉到」的东西：行为变化、需要你做的事、修了什么问题。

## 2026-09-20

- **升级后不再需要手动处理类别配置**。以前升级完，`data/config/categories.json` 不会被更新（它是你的配置，程序从不覆盖它），所以老用户看不到新增的内置类别（比如「债券」「现金」）—— 添加基金时选不到，环形图里这类基金的市值还会落到「未归类」，而且不会报错。现在程序会在**启动时自动把缺的内置项补上**：只补缺的，**绝不覆盖你改过的名字、也绝不动你自建的分类**；补之前先备份一份到 `data/config/categories.json.bak-时间戳`，启动日志里会打印补齐了哪些。**你什么都不用做**
- 修复：更新日志里对「类别管理」的描述与实际界面不符，已在下文该条内更正

## 2026-09-19

- **首次运行不再预置示例持仓**：`npm run setup` 生成的是**空看板**（`funds` 为空数组），不再带任何示例基金。启动后到「持仓」页添加你自己的第一只即可 —— 仍然不需要手改任何 json
- 五个页面都补了空状态引导：概览 / 决策 / 持仓 / 配置 / 复盘会直接告诉你「还没有基金 → 去持仓页添加第一只」，而不是只显示一句「暂无」
- `npm run setup` 的收尾提示改为说明「已生成空看板」，不再让你手动去改 `data/config/config.json` 与 `data/state/holdings.json`
- 内部整理：清掉了早期模板里残留的示例基金代码与失效配置项（`defensive` 段已删除 —— 它没有任何代码在读取）

### 2026-09-19 · 第二次更新（添加基金这条主链路）

- **添加基金的自动识别大幅加强**：填一个基金代码，现在会从基金档案里带出**基金类型、跟踪指数、申购状态**，并据此**自动选好类别**。以前只按名称关键词猜，猜不到就一律默认成「科技成长」—— 那会把债基、消费基金套上错误的算法，而且**不会报错**，只会给一个看起来正常的错结论
- **类别名称改得能看懂适用范围**：「科技成长」→「主题·行业（高波动）」、「黄金(对冲)」→「商品·对冲」。这两个名字以前把适用范围说窄了 —— 实质上前者适用于任何高波动行业（医药/消费/新能源/军工都算），后者适用于任何商品（白银/原油/豆粕都算），不只是科技和黄金
- **新增两类可记账的类别：债券、现金**。它们的**决策算法还没做**（待建设），所以看板上会明确显示「待建设 · 暂不判定」，只计入总资产与配置占比，**不给买卖结论** —— 而不是像以前那样把不认识的基金静默丢掉
- **缺估值锚会被明确告知**：有些基金（如创业板、医药指数基金）跟踪的指数我们没有免费估值源，此时判定会降级为价格分位。以前界面只是默默显示 `?` 并恒定建议「持仓不动」，看着像在正常工作；现在会打出「缺估值锚·降级」标记并说明原因
- **配置页新增「类别管理」**：可以**新增自建分类**（给某条内置算法起个别名，比如把「主题·行业」另叫成「我的医药」），也能删除**自己新增的**。内置预设是只读展示的，**不可改名、也不可删除**。自建分类**不是新算法**，只是别名 —— 新增时要选一条内置算法去绑定，用哪条算法由它决定
- 修复：配置页环形图遇到不在类别表里的基金时会**丢掉它的市值**，导致各段占比之和不到 100%（只看到"钱数对不上"、没有任何提示）。现在保证各段之和恒等于 100%
- ⚠️ **升级须知（只影响"想看新类别"的人）**：`data/config/categories.json` 是**不会被 setup 覆盖**的（这是设计，防你的配置被冲掉）。所以老用户升级后**不会**自动多出「债券 / 现金」两个选项。想用的话：删掉 `data/config/categories.json` 再跑一次 `npm run setup`（会重置该文件的类别配置，**持仓数据不受影响**），或手动把 `data/example/categories.example.json` 的 `categories` 段照抄过去
  > **2026-09-20 起本条已作废**：改为启动时自动补齐，不再需要删文件或手抄（见顶部当天条目）
- **升级方式不变**：仍是 `git pull` 后重启。从本次起，若某次更新改了数据结构，程序会在启动时**自动处理**——处理前先自动备份一份到 `data/` 下的 `*.bak-时间戳` 文件，你不需要做任何事，也不需要会备份
- 新增 `npm run check:offline`：确认离线测试链里没有混进需要联网的检查（此前有一条检查会悄悄联网，在境外机器上会误报失败）
- 修复：日期推算在非东八区的机器上会整体差一天的 bug（在国内使用感觉不到；部署到境外服务器或 CI 时会踩到）

## 2026-09-18

- 修复：需要联网的那条检查（现在叫 `npm run test:network`）此前被误放进离线测试链，导致境外机器的 CI 稳定误报失败
- 新增贡献者指南（`CONTRIBUTING.md`）与 PR 模板
- Windows 首次运行流程进入 CI：真跑 `setup.bat` + 幂等性断言（第二次运行绝不覆盖你的数据，这条承诺从此有机器盯着）
