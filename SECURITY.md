# 安全策略

## 报告漏洞：请走私密渠道

**不要开公开 issue。** 用 GitHub 自带的私密漏洞报告：

1. 打开本仓库的 **Security** 标签页
2. 点 **Report a vulnerability**
3. 写清：受影响的提交（`git log -1 --oneline`）、复现步骤、影响面、你建议的修复方向

确认收到后会尽快回复。在修复发布之前，请不要公开细节。

## 什么算漏洞（帮你判断优先级）

这是一个**只跑在你自己电脑上**的应用：没有云端、没有账号体系、没有联网上传。所以真正值得报的问题大致是这几类：

| 类型 | 说明 |
| --- | --- |
| 写接口鉴权被绕过 | `/api/save`、`/api/purchase`、`/api/theme-map`、`/api/backfill-pending` 靠 `X-API-Key` 校验。能绕过它改数据 = 真漏洞 |
| 读接口泄露写密钥 | `/api/state` 下发前会剥掉 `apiKey`。若某条读接口把它带出去，等于同网段任何人都能改你的数据 |
| 路径穿越 / 任意文件读写 | 静态托管与数据接口对路径的处理 |
| 命令执行 / 原型污染 | 运行时零第三方依赖（只用 Node 内置模块），但反序列化与对象合并逻辑仍可能有洞 |
| 抓取结果被投毒导致错误结论 | 本项目明确拒绝「静默算错」：可疑数据应显式降级，而不是给出一个看着正常的错答案 |

## 什么**不算**漏洞（这些是有意行为）

- **服务默认监听所有网卡**（同一 WiFi 下的手机/平板可访问）。要仅本机访问，设 `HOST=127.0.0.1`
- **数据明文存在本地 `data/`**。这是「全部数据在你本机、不上传」的代价与承诺
- **抓不到数据时降级显示**（显示缺失或兜底值）。已有兜底与降级标注，也不会伪造数据
- **写接口默认口令是 `dev`**。这是为了让刚 clone 的人开箱就能保存数据；要暴露给同网段其他设备用之前，请先换掉它

## 使用者的自我保护

- **别把服务直接暴露到公网。** 要给局域网之外的设备用，请套一层反向代理并加你自己的鉴权
- 把 `data/config/config.json` 里的 `apiKey` 从 `dev` 换成一个自己的随机串，或改用环境变量 `FUND_API_KEY`（优先级更高，口令就不落盘）
- `data/state/` 是唯一「丢了找不回来」的目录，自己定期备份
- 密钥不要写进任何会被 `git` 跟踪的文件
- 报 bug 时不要粘贴你自己的金额、份额或持仓截图 —— 给基金代码就够了

## 支持范围

只维护仓库当前的 `main` 分支。本项目是个人维护的开源工具，没有 SLA，但安全问题会优先处理。
