# 参与开发

谢谢你来一起改这个看板。这份文档只讲**你实际会用到的东西**，不写客套话。

## 0. 先跑起来

```bash
git clone https://github.com/pharos-studio/pharos-open.git
cd pharos-open
npm run setup     # 生成你自己的数据文件（幂等，不会覆盖已有数据）
npm start         # http://localhost:3000
```

**不需要 `npm install`** —— 本项目零运行时依赖，只用 Node 内置模块。Node 要 **18 或更高**（用了全局 `fetch`）。

改完代码一定跑一遍：

```bash
npm test          # 生成数据文件 → 9 组离线校验 → 脱敏审计，全程不联网
```

`npm test` 全绿是提 PR 的前提。

> **别把需要联网的检查加进 `test:offline`。** CI 跑在境外 runner 上，连不上东财/蛋卷这类国内数据源 ——
> 加进去会**稳定误报失败**，而且报的是「断言不过」，看不出真正原因是网络。本项目已经踩过一次：
> `verify_bond10y` 在本机能通、秒回，看着像离线，实际是联网的。
> **加脚本进离线链之前，先跑 `npm run check:offline`** —— 它会给每个脚本挂上断网器复跑一遍，
> 联网的会当场被抓出来。

---

## 1. 分支与提交

从 `main` 切分支，**不要直接推 `main`**（分支保护会拒绝）。

| 前缀 | 用在什么地方 | 例子 |
| --- | --- | --- |
| `feat/` | 新功能 | `feat/broad-cn-decision` |
| `fix/` | 修 bug | `fix/settle-date-roll` |
| `docs/` | 只改文档 | `docs/readme-first-run` |
| `test/` | 只加/改测试 | `test/nav-parallel-cache` |
| `chore/` | 杂活（配置、脚本） | `chore/ci-node-matrix` |

提交信息用一句话说清「改了什么」，例如 `fix: 非交易日下单不再顺延到下一交易日`。

---

## 2. PR 流程

1. 推分支 → 在 GitHub 上开 PR（模板会自动出现）
2. 填清楚：**改了什么 / 为什么 / 怎么验证的**
3. 等 CI 变绿：`离线校验`（Node 18 与 22）和 `Windows 首次运行` 两个 job 都必须过
4. 对方 approve 后合并（两人可以互批，不需要等特定人）
5. 合并方式用 **Squash**，让 `main` 保持线性

---

## 3. 平台差异

| 操作 | Windows | macOS / Linux |
| --- | --- | --- |
| 生成数据文件 | 双击 `setup.bat` | `npm run setup` |
| 启动服务 | 双击 `start.bat` | `npm start` |
| 换端口 | `set PORT=3001 && npm start` | `PORT=3001 npm start` |

改 `.bat` 时务必守住两条：

- **纯 ASCII。** 里面混进中文再配合 `goto` / `call`，cmd 会把半个汉字当成命令，脚本会以难以理解的方式挂掉。改 `.bat` 请只写英文注释。
- **换行交给 `.gitattributes`。** 不要手工转换整个文件的换行符 —— 那会造出「每一行都变了」的假 diff，也会掩盖真正的改动。

---

## 4. 数据目录：哪些能提交，哪些绝对不能

`data/` 按角色分五区。目录映射的**唯一真相源是 `backend/lib/store.js` 的 `LAYOUT`**（改目录结构只改那张表，别在别处拼 `data/xxx` 路径）：

| 分区 | 是什么 | 进 git 吗 |
| --- | --- | --- |
| `data/example/` | 脱敏模板（演示数据） | ✅ 进，本来就是要给大家看的 |
| `data/config/theme_map.json` | 赛道词典 | ✅ 进 |
| `data/state/` | 持仓 / 每日快照 / 决策记录 | ❌ **永不** |
| `data/series/` | 自建时序序列 | ❌ **永不** |
| `data/cache/` | 派生缓存 | ❌ **永不** |
| `data/config/config.json`、`categories.json` | 运行配置 | ❌ **永不** |

`data/state/` 是唯一「丢了找不回」的目录，而且它只在你本机 —— 请自己定期备份。

加了新的数据文件时，记得**同时**改两处：`store.js` 的 `LAYOUT` 和 `.gitignore`。

---

## 5. ★ 脱敏红线（违反这一节会出真事故）

本仓库是公开的。**任何一次「顺手」提交，都可能把某个人的真实账目摊到公网上。**

- **绝不提交**：真实持仓、真实金额、真实份额、真实净值、本机绝对路径、API Key、真实 IP
- **测试要用的真实数值，一律放进本地夹具** `data/state/regression_cases.json`（已被 `.gitignore` 忽略）。脚本读不到夹具时会打印 `SKIP` 并**以退出码 0 通过**，所以两仓可以用同一份脚本，不会误报失败
- ★ **脱敏说明本身也不许举真实例子。** 写「把某个金额替换成 X」时，如果那个金额是真的，它就已经泄露了。举例子请用**构造值**，或写「样本基金」这类中性词
- 提交前跑 `npm run audit`。它分三档查：**数值指纹**（小数位 ≥ 4 的净值/份额）、**关键词**（本机路径、用户名、隐藏工具目录）、**内联 lint**（写死的 `nav` / `shares` 数值字面量）。出现 `FAIL` 就是**一个字节都不许推**

> 为什么这么严：本项目真的发生过一次。一位维护者的几笔真实买入被当成测试的「标准答案」写死在脚本里，推到公网后可直接读取。当时的脱敏只查了关键词，没查**数值** —— 所以现在两者都查，并且把「数值指纹」当成了主要判据。

---

## 6. ★ 同步纪律：不要「两边各改一半」

维护者的机器上另有一份私有的「数据机」仓库，只用来跑真实数据、存放不公开的私人材料。两仓之间靠一份**显式清单**同步，清单在私有侧。

**作为贡献者，你的规则只有一条：公共代码改在这个仓库。**

- 要改 `backend/`、`public/`、`data/example/` 或公开文档 → **就在本仓库改，走 PR**
- 别指望「维护者在另一台机器上已经改好了」：共享代码的唯一真相源就是本仓库
- **同一个文件不要出现两份各改一半的副本。** 要改就整份改，一次改完

维护者侧的动作顺序是：**先合并你的 PR → 把本仓库同步回数据机 → 再从数据机向本仓库发布**。发布这一步不会自动拉取你的成果，所以「先回同步、再发布」漏一步，你的改动会被静默覆盖 —— 这是这条纪律存在的唯一原因。

---

## 7. 改算法时的四个「唯一真相源」

同一个口径只允许有一份实现。复制一份出来，迟早会算出两个不同的答案，而且不会报错。

| 口径 | 唯一真相源 | 别干的事 |
| --- | --- | --- |
| 交易日 / 定价日 / 确认日 | `backend/lib/tradeDate.js` | 不要在前端或别的脚本里自己算日期 |
| 份额与净投入公式 | `backend/lib/buyPlan.js` | 前端不要自己乘净值算份额 |
| 净值缓存与取数 | `backend/lib/navQuote.js` | 不要绕过它直接请求数据源 |
| `data/` 目录映射 | `backend/lib/store.js` 的 `LAYOUT` | 不要在别处拼 `data/xxx` 路径 |

另有两条硬规矩：

- **净值取值必须按「不晚于当日的最大日期」取**，不能假设历史序列是升序（实际是降序）。假设错了不会报错，只会静默算出一个错的份额
- **改阈值前先跑回测**：`backend/scripts/backtest_*.js`。这类阈值不能套行业惯例，只能从该资产自身的历史反解出来

### ★ 写日期代码：一律 UTC 锚点 + UTC getter

`YYYY-MM-DD` 是**日历日**，不带时刻。别把它解析成带时区的瞬间（如 `+08:00`）之后，又用**本机时区**的
`getDate` / `setDate` / `getDay` / `getFullYear` 去读写 —— 那个组合只在东八区恰好正确，
进程一跑到 UTC 就**整体早一天**，而且不报错。正确写法：

```js
const d = new Date(dateStr + 'T00:00:00Z');   // 不要 +08:00
d.setUTCDate(d.getUTCDate() + 1);             // 不要 setDate/getDate
```

**「今天是几号」要用 `util.todayStr()`**（它按上海时区算），不要用 `new Date()` 自己格式化 ——
业务时区是上海，进程时区是另一回事。铁律与本次事故的完整说明写在 `backend/lib/tradeDate.js` 顶部。

> **CI 刻意跑在 UTC**，不设 `TZ`。这不是疏忽，是让它当**时区哨兵** —— 上面这个 bug 就是它抓到的。
> 将来若有人图省事给 CI 加上 `TZ=Asia/Shanghai`，这个哨兵就失效了，别加。

---

## 8. 提 PR 前的自检

```bash
npm test                               # 必须全绿
git ls-files -ci --exclude-standard    # 必须为空（没有数据文件被误入库）
```

再加肉眼一关：`git diff --cached` 从头看一遍，**每一行**都问自己一句 —— 这行会不会泄露谁的真实数据？

---

## 9. 有疑问直接问

在 PR 或 issue 里问，不要猜。宁可多问一句，也别让一个「我觉得应该没问题」进 `main`。
