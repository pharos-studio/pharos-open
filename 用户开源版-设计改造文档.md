# 用户开源版 · 设计改造文档

> 目标仓库：`pharos-open`（公开仓）｜私有仓 = 数据机（真实数据，永不外传）
> 写作依据：本仓代码实测 + 东财接口实测 + 私有仓 `docs/版本更新流程.md` 与 `publish.manifest.json`

---

## 一、产品契约（这次改造要兑现的唯一一件事）

> **用户 clone → `npm run setup` → 在前端手动添加自己的基金 → 概览/决策/持仓/配置/复盘五页内容自动全部显示，完全不碰任何 JSON。**

私有仓与公开仓共用同一份 `shared` 代码，不分叉。

---

## 二、现状诊断：三个「静默错误」

改造前，四个页面都不会崩，但会**给出看起来正常的错结论**——这比报错危险得多。

| # | 症状 | 证据 | 后果 |
|---|---|---|---|
| 1 | 类别被兜底猜成「主题·行业」 | `public/js/pages/holdings.js` 的 `suggestCategory()` 末句 `return 'growth'`（2026-09-23 拆分后为 `holdings/fundMeta.js`） | 用户加债基/消费/医药 → 套 `tech.js` 的回撤抄底算法 → 不报错，就是错 |
| 2 | 跟踪指数用户填不了 | 表单原无该字段；只有 5 条名称正则 | 缺锚 → `analysis.js` 估值退化 → `kernel.js` `peZone='na'` → **action 恒 hold**，决策页显示「持仓不动」，像在正常工作 |
| 3 | 未知类别被静默剔除 | `advice.js` `if(!hit) continue`；`analysis.js` 的 `byCat` 只映射 `categories` 里的键 | 决策页整只消失；配置页环形图丢掉它的市值 → **各段占比之和 <100%**，用户只看到"钱数不对" |

另有一批作者遗留（已在本次清理）：示例持仓的 9 只真实基金、`dailyLimits`/`peFallback`/`fundThresholds` 按真实代码逐只标定、`defensive.funds` 僵尸配置、代码注释里散落的真实代码。

> **★ 2026-09-20 补充：还有第四个静默问题**，不在上表里 —— 它不在"页面算错"这条线上，而在**升级路径**上：阶段三新增的内置类别进不去老用户的配置文件，用户只表现为"下拉里没有债券/现金"，同样不报错。修法与实测见 §四 的补丁小节。

> **★ 2026-09-23 补充：第五个静默问题** —— 既有"页面算错"的成分、也有"配置进不去"的成分：`feeRate`（申购费率）**算法一直在用**（算净投入、推份额、预览买入），但**界面上从来没有输入口**，前端 4 处引用全是硬编码 `0` ⇒ 系统一直按「零申购费」算，用户以为系统知道自己实付了多少，实际上从没知道过。修法与实测见 §四 的补丁小节。

---

## 三、分类体系设计

### 3.1 关键认知：现有五条算法的适用范围**比名字宽**

| 算法 | 表面名字 | 真实适用范围 | 为什么 |
|---|---|---|---|
| `core.js` | 宽基（沪深300） | **任何 A 股宽基指数** | 锚 = 该指数自己的 PE 分位 × 中债 ERP |
| `broadGlobal.js` | 宽基·海外 | **任何海外宽基** | 滚动 156 周分位 ∨ PE 回撤 |
| `dividend.js` | 红利低波 | **仅 A 股红利** | 参考带是中证红利 000922，海外红利无免费源 |
| `tech.js` | 科技成长 | **任何高波动资产**（医药/消费/新能源/军工/主动偏股） | 只看基金自身净值的 60 日回撤 + 止跌 + 双均线，**不看行业、不看指数** |
| `gold.js` | 黄金(对冲) | **任何商品**（白银/原油/豆粕） | 只看自身净值的 250 日价格分位，**不绑黄金** |

→ 所以「科技成长」「黄金」这两个显示名**把适用范围说窄了**，用户不敢把医药/原油基金放进去。本次已改为「主题·行业（高波动）」「商品·对冲」，并在策略文件头部写明适用范围。

### 3.2 真缺口只有两个，且与前四条本质不同

- **债券型**：套哪条都不对。套 `tech.js` 要跌 15% 才抄底（债基几乎永不触发）；套 `core.js` 又没有 PE 可算。正确锚应是「10 年国债利率分位 + 信用利差」——完全不同的数据源与逻辑。
- **货币/现金**：根本不需要买入信号，只该「计入总资产 + 占比展示」。

**本轮决定：两者都只做「占位分类、暂不提供算法」**，看板显式显示「待建设」而不是给结论，也绝不静默消失。

### 3.3 自动归类：改用基金类型（FTYPE），不再靠猜

**实测**东财基金档案接口，字段覆盖完整（`{code}` 替换为基金代码）：

```
https://fundmobapi.eastmoney.com/FundMNewApi/FundMNNBasicInformation?FCODE={code}&deviceid=Wap&version=2.0.0&product=EFund&plat=iPhone&osVersion=16.6&appType=iPhone
```

| 基金 | `FTYPE` | `INDEXCODE` | `INDEXNAME` | 风险 | 申购 |
|---|---|---|---|---|---|
| 202015 南方沪深300ETF联接A | 指数型-股票 | `000300` | 沪深300指数 | 4 | 开放申购 |
| 100032 富国中证红利指数增强A | 指数型-股票 | `000922` | 中证红利指数 | — | — |
| 001593 天弘创业板ETF联接C | 指数型-股票 | `399006` | 创业板指数(价格) | — | — |
| 001550 天弘中证医药100A | 指数型-股票 | `000978` | 中证医药100指数 | 4 | 开放申购 |
| 016452 南方纳斯达克100(QDII)A | 指数型-海外股票 | `NDX100` | 纳斯达克100指数 | 4 | **限大额** |
| 008163 南方标普红利低波50联接A | 指数型-股票 | `SPCLLHCP` | 标普中国A股大盘红利低波50指数 | — | — |
| 018391 南方上海金ETF联接A | 指数型-其他 | `SHAU` | 上海金 | 4 | 开放申购 |
| 016874 广发远见智选混合C | 混合型-偏股 | `--` | `--` | 4 | 开放申购 |
| 217022 招商产业债券A | 债券型-混合一级 | `--` | `--` | 2 | **限大额** |
| 000198 天弘余额宝货币 | 货币型-普通货币 | `--` | `--` | 1 | 开放申购 |

结论：
1. **`FTYPE` 是可枚举的确定值**，覆盖全部类型 → **自动归类是确定性问题，不需要猜**。
2. **`INDEXCODE` 是机器可读键**，比按中文名匹配稳。
3. **`"--"` 是空值陷阱**：非指数基金返回字符串 `"--"`，必须当空处理，否则会被当成合法指数去匹配。
4. **★★ 这个接口会拦桌面浏览器 UA**（本节最重要的一条）：用项目默认的桌面 Chrome UA 请求，会**稳定**返回
   `{"Datas":null,"ErrCode":61136403,"ErrMsg":"网络繁忙，请稍后重试！"}` —— 看起来像"接口挂了"，其实只是 UA 不对。
   实测对照：**iPhone / Android / curl / 空 UA 全部放行，唯独桌面 Chrome 被拦**。
   → 所以 `fetchFundArchive` **必须显式传移动端 UA**，不能沿用 `lib/http` 的共享 UA（那个正是桌面 Chrome）。
   > 踩坑记录：最初用 curl 测是通的，差点据此得出"不需要伪装头"的错误结论 ——
   > 真相是 **curl 的默认 UA 恰好被放行**。这条只有换用项目真实 UA 复现才会暴露。
5. **`SGZT`（申购状态）是白送的**：可直接喂给 `dailyLimits`/`suspended` 语义（QDII 限购是用户最容易踩的坑）。

### 3.4 内部指数白名单（只有这 5 个有估值源）

`INDEXCODE` → 内部键。**清单外一律返回 null → 走显式降级**（宁可说算不准，也不假装能算）：

| 东财 `INDEXCODE` | 内部键 | 说明 |
|---|---|---|
| `000300` | `SH000300` | 沪深300（乐咕取 PE 分位） |
| `000922` | `SH000922` | 中证红利（蛋卷取股息率） |
| `000993` | `SH000993` | 全指信息 |
| `NDX100` | `NDX` | 纳斯达克100（蛋卷取 PE 历史） |
| `SPCLLHCP` | `CSI930955` | ★**代理映射**：标普红利低波50 无免费源，用中证红利低波作因子代理（人工确认过） |

**故意不登记、会走降级的例子**：创业板指 `399006`、中证医药100 `000978`、上海金 `SHAU`。
→ 这是本次的已知边界：买了创业板/医药指数基金的用户会看到「缺估值锚·降级」，而不是一个假结论。**这是有意的诚实。**

### 3.5 最容易踩的坑：两个概念必须分清

| 概念 | 含义 | 由谁决定 |
|---|---|---|
| `trackIndex` | 这只基金**跟踪什么指数**（纯记录，谁都可以有） | 基金本身 |
| `usesIndexAnchor()` | 这条策略线**能不能**用指数估值锚 | **策略线** |

**为什么必须分开**：`analysis.js` 里曾用 `!f.trackIndex` 当作「无指数估值 → 走价格分位弱信号」的判据。而东财确实会给黄金基金返回指数代码 `SHAU`——**一旦自动抓取把 `trackIndex` 填上，那个判据会被误触发，商品线的行为就变了**（黄金是四条线之一，属行为回归）。

→ 铁律：**能不能用锚永远由策略线决定，绝不由「有没有抓到指数」决定。**

能力表（`needsTrackIndex` / `usesIndexAnchor` 实际取值）：

| 策略线 | 需要指数锚（缺了会退化，必须提示） | 可以用指数锚 |
|---|---|---|
| `broad` 宽基 | ✅ 是 | ✅ 是 |
| `dividend` 红利低波 | ✅ 是 | ✅ 是 |
| `growth` 主题·行业 | ❌ 否（自身价格分位兜底） | ✅ 是（有则更准） |
| `cycle` 商品 | ❌ 否 | ❌ **永远否** |
| `bond` / `cash` | — | ❌ 永远否 |

### 3.6 指数身份优先于基金类型

实测发现 `FTYPE` 只会说「指数型-股票」，**分不出「红利指数」和「宽基指数」**：富国中证红利(`000922`) 与红利低波50(`SPCLLHCP`) 都会被归成宽基、套错算法。
而指数身份是明确的 → **当跟踪指数解析成功、且它的策略线比 FTYPE 更具体时，由指数身份覆盖**（`by='index'`）。

---

## 四、分阶段改动清单与状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| **前置** | 移走本地产物（`data/state/`、`data/config/{config,categories}.json`） | ✅ 已完成（备份在 `X:\pharos\_verify_backup_20260919\`） |
| **一** | 清作者遗留 + 空看板 + 首次引导 | ✅ **已完成并端到端验证** |
| **二** | trackIndex 一等公民（自动抓 + 可手填 + 诚实降级） | ✅ **已完成**（含 F1 闸门，回归测试通过） |
| **三** | 分类扩预设 + 自建 + 待建设显式状态 | ✅ **已完成并端到端验证** |
| **四** | README / CI / 社区基建一致性 | ✅ **已完成**（见下） |
| **补丁** | 升级时自动补齐缺失的内置类别（2026-09-20） | ✅ **已上线并端到端验证**（见下） |
| **补丁** | 申购费改为自动抓取、界面只读（2026-09-23） | ✅ **已上线并端到端验证**（见下） |
| **五** | 一键添加的「我的基金」页 + 每笔买入积分抵扣（2026-09-24 定方案） | 📋 **计划中**，尚未实施（见下） |

### 补丁（2026-09-20）：升级自动补齐内置类别

**这是阶段三留下的一个真实缺口**，也是 §二 那三个静默错点之外的**第四个**：阶段三把内置类别从 4 条扩到 6 条（+债券/现金），但这些内置项**只存在于模板文件** `data/example/categories.example.json`，而模板**只在 setup 时**被整份拷成 `data/config/categories.json`，setup 又是**幂等**的（已存在即跳过）。用户升级只做 `git pull` + 重启、不会重跑 setup → 新内置项**永远进不去**他的文件 → 「添加基金选不到债券/现金」「环形图里这类基金的市值落进未归类」，且不报错。

| 位置 | 改动 |
|---|---|
| `backend/lib/categories.js`（**新增**） | 内置项**唯一真相源**：展示线 6 / 可绑定算法 4 / 口径 2 / 预设 7 + `ensureBuiltins()` **纯函数**（不读盘不写盘、不改入参）。按 key(presets 按 id) 判重，**只把缺的追加到末尾**，不重排不删不改已有条目；幂等（二次 `changed:false`）；边界一律不抛错；`customCategories` 属用户层，不合成不触碰 |
| `backend/lib/schema.js` | 新增并导出 `backupFile(full)`；`migrateIfNeeded` 改为复用它 → **备份命名只留一份实现**（零行为变化） |
| `backend/server.js` | ① `BASE_CATEGORIES` 改引用 `lib/categories`（值不变，仍是**六条展示线**；⚠ 不可改成四条 engines —— `/api/save` 的 `okBind` 白名单依赖这六条）；② 启动新增 **L9.5**：文件不存在则跳过；有变化才**先备份**再写盘，并打印补齐了哪些；读/写失败**只 warn 不退出** |
| `backend/scripts/verify_caliber_routing.js` | 追加 **23 条断言**（含「代码常量 == `categories.example.json`」的**防漂移**断言）。刻意**不新增第 10 个离线脚本**——那会连带改 `package.json` 与 `ci.yml` 的组数注释，而本机 token 无 `workflow` 权限推不动工作流文件 |
| `CHANGELOG.md` | 新增当天条目；更正 09-19 那条对「类别管理」的失实描述（实际是：可新增/删除**自建**分类；内置预设只读，**不可改名也不可删除**） |
| `public/js/pages/allocation.js` | 「缺预设段」的提示改为「重启一次服务即可自动补齐」 |

**为什么 L9.5 失败只 warn、不像 L9 那样 fail fast**：L9 那条 fail fast 的前提是「带着**旧结构**静默跑新代码 → 会算错」；而这里失败的后果**仅仅是「仍然看不到债券/现金」**，与修补前逐位相同、不产生任何错误结论，且已明确打印告警（不是静默）；写失败天然幂等，下次启动重试。**L9 的 fail fast 保持不变。**

**实测验收**（全部通过）：构造旧版 4 条文件 → 启动打印
`[categories] 已补齐内置项：展示线/bond（债券）、…（原文件已备份：categories.json.bak-…）`，
`/api/state` 下发的 `categories` 变成 6 条、`presets` 7 条，备份文件里是**原始 4 条**；
**二次启动零日志且文件 sha 不变**；用户改过的显示名（`broad→我的大盘`）与自建分类 `custom:ab12`
一字未动、`customCategories` 未被合成；债基在环形图**独立成块、占比之和 = 100.00%**。
`npm test` exit 0（9 组离线 + 脱敏审计 FAIL 0 / WARN 8 基线）、`TZ=UTC` 复现通过、远端 CI ✅ success。

**顺带解决的一个推送障碍**：本地曾有提交 `30a9b12` 只改 `.github/workflows/ci.yml` 的 3 行注释，被 GitHub 以「token 无 `workflow` 权限」拒收；而它是**任何新提交的祖先**，留着会让后续提交也推不上去。已用 `git reset --hard 870dfaa` 摘掉（那 3 行注释若要修，可在 GitHub 网页直接改第 28/38/43 行，**不需要任何 token**）。

### 补丁（2026-09-23）：申购费自动抓取 + 界面只读

**这是 §二 补充所说的第五个静默问题。** `feeRate` 在算法里是活的（`lib/buyPlan.js` 的 `validFeeRate` / `computeShares` / `netInvestedOf`，以及买入预览），但**前端从没有过输入框** —— `public/js/` 里 4 处 `feeRate` 全是硬编码 `0`。于是「实付 ≡ 净投入」「累计申购费 ≡ 0」，而且**不报错**：用户看不到任何异常，只是成本口径悄悄失真。

本次把它从「用户输入」改成「**基金属性**」：后端抓取写入，界面只读。

| 位置 | 改动 |
|---|---|
| `backend/fetchers.js` | 新增 `fetchFundRates(code)`：东财移动接口 `FundMNRateInfo` → `sg`/`rg`/`sh` 三套分档 + `MGREXP`/`TRUSTEXP`/`SALESEXP` 运作费 + `SGZT`/`SHZT` 申赎状态。★ 必须用移动端 UA（桌面 UA 会返回 HTTP 200 + 业务错误码 `61136403`，与档案接口同一拦截码）；★ 「1000元/笔」这类**定额档**折算不出百分比，只留 `raw` 原文（直接转数字会得到 1000） |
| `backend/engines/feeSync.js`（**新增**） | `feeRate` / `feeDetail` 的**唯一写入方**。30 天有效期（`RATE_TTL`）；四触发点全幂等：启动延迟 3s / 每 24h / `/api/save` 检出新增基金立即（3s 封顶）/ `POST /api/fees/refresh`（`?force=1` 强制）。★ 抓不到**保留原值**，绝不用 0 覆盖（`0` 是「免申购费」的语义）；`in-flight` 锁 + pending 补跑，避免两次同步同时写盘 |
| `backend/server.js` | 新增 `pinFundFees(incoming)`：**写盘前**用磁盘值把回传的费率字段盖回去（与 `restoreSecrets` 同一范式）；磁盘上没有的基金（本次新增）**直接剥掉**该字段、交给 feeSync 抓——★ 不能留着前端那个 `feeRate: 0`，否则会被误当成「已知的 0」而跳过抓取。新增 `POST /api/fees/refresh`（鉴权同 `/api/save`）；启动/定时接线（定时器 `unref`，不阻塞进程退出） |
| `backend/engines/analysis.js` | 输出对象补 `feeRate` / `feeDetail`（供前端展示） |
| `public/js/pages/holdings/fundMeta.js` | 新增 `feeNote(f)` → `{ text: '申购费 0.12%', title: '法定原价 1.2%（1 折）· 数据源 天天基金 · 更新 09-23' }` |
| `public/js/pages/holdings/{desktop,mobile}.js` | 在基金 meta 行追加只读文本 + `title` 提示（**无输入框**） |
| `public/sw.js` | `v25 → v26`（改了 `pages/holdings/` 下的模块，不升版本用户首屏仍见旧页） |

**★ 与收益口径的关系（最要紧的一条）**：补齐费率**不改写已确认的买入记录** —— `lib/buyPlan.js` 的净投入对已确认记录（有 `shares` + `nav`）直接取 `shares × nav` 券商真值，只有**在途**（份额未确认）才用 `amount × (1 − feeRate)` 预估。**实测（9 只真实持仓、37 笔已确认记录）：补齐前后净投入合计差 `0.000000`** ⇒ 历史盈亏数字不变，只影响将来记新一笔时的份额推导与在途预估。

**实测验收**（全部通过）：9/9 只基金抓取成功（含 2 只 C 类，正确地返回 `0` = 免申购费）；**攻击式验证** —— ① 伪造已有基金 `feeRate = 9.99` 保存 → 磁盘保持原值、注入的 `hacked` 字段未落盘；② 提交里**删掉**费率字段 → 磁盘未被抹掉；③ 新增基金带假费率 `9.99` → 字段被剥离并由 feeSync 立即抓回真值（原价 1.20% / 折后 0.12%）；④ 无鉴权 `POST /api/fees/refresh` → 401。前端零输入框（`grep` 确认）。`npm test` exit 0、脱敏审计 `FAIL 0 / WARN 9`（与基线持平）。真实浏览器桌面 1440×900 / 手机 390×844 双视口渲染「申购费 x%」正常、零 console 异常。

### 阶段四已完成的具体改动

| 位置 | 改动 |
|---|---|
| `docs/后端架构清单.md` | 「仓库已转为**私有**」→ 中性措辞（该文件是 `shared`，旧措辞在公开仓里是错的）；断链改「内部文档，未随开源发布」 |
| `backend/engines/timing.js`、`backend/server.js`、`backend/lib/tradeDate.js` | 注释里指向 3 篇未发布私有文档的断链 → 中性措辞 |
| `backend/scripts/{fix_nav_caliber,make_checklist,backtest_composite_weights,duty_cycle_monitor}.js` | 同上 |
| `data/example/config.example.json` | `composite._note` 里的私有文档断链 → 中性措辞 |
| `backend/server.js` | **新增 `HOST` 环境变量支持** + 启动时打印局域网地址（手机连不上的排查靠它） |
| `README.md` | ① 「服务只监听本机，不对外暴露」是**错的** → 改为「默认监听所有网卡（手机同 WiFi 靠它）；`HOST=127.0.0.1` 可仅本机」；② 新增「**第一次打开：加你的第一只基金**」章节；③ 新增「**参与贡献**」章节（指向 `CONTRIBUTING.md`）；④ 修正「数据机**不反过来覆盖**本仓库」这句与 `CONTRIBUTING.md` 第 6 节的矛盾——按清单机制，清单内文件方向是**双向**的，漏掉「公开仓 → 数据机」这一步就会被旧版本静默覆盖 |
| `index.html`（介绍页） | 旧线名（科技成长/黄金对冲/红利低波）→ 新名；「算法四线」→「策略线」并补债券/现金 |
| `.github/workflows/ci.yml` | 注释「8 组」→「9 组」（与 `package.json` 实际的 9 个离线脚本对齐，共 3 处） |
| `SECURITY.md`（新建） | 漏洞走私密渠道上报；列清「什么算漏洞 / 什么是有意行为」；使用者自我保护清单 |
| `.github/ISSUE_TEMPLATE/{bug_report,feature_request}.md`（新建） | 两个模板，都明确提示**别粘贴真实持仓/金额** |
| `backend/scripts/make_checklist.js` | 输出路径指向内部文档这件事**写进注释**（它落在 `docs/`，而公开仓 `docs/*.md` 走白名单 → 提交上去会被审计判 FAIL，属于必须让人看得见的坑） |

⚠️ 阶段四**刻意不动**：`data/config/config.json`（privateOnly，数据机自持）、`.gitignore` / `package.json` / `package-lock.json`（`intentionalDivergence`，两仓故意不同）、`docs/` 下未新增任何 `.md`（`DOCS_ALLOWED` 只放行 4 篇）。

> 自查记录：第一次写这份文档时，我把维护者的 GitHub 账号名写进了表格，被脱敏审计判 `stale-url` WARN —— **`CONTRIBUTING.md` 那句「脱敏说明本身也不许举真实例子」说的就是这类错误**。已改为不登记私有仓地址。

### 阶段一已完成的具体改动

1. `data/example/holdings.example.json` → `funds: []`，保留匹配 `DEMO DATA|NOT real holdings|placeholder` 的注释
2. `backend/lib/schema.js` `normalizeHoldings` → **`funds` 非空即剥离示例标记**（否则脱敏审计会永久停在 Mode B，门禁静默失效）
3. `data/example/config.example.json` → 清空 `dailyLimits`/`peFallback`/`fundThresholds`，删除僵尸配置 `defensive`，清洗 5 处 `_note` 里的真实代码
4. 生产代码注释清洗：`strategies/{core,tech,dividend,gold}.js`（含新增适用范围说明）、`fetchers.js` × 7、`advice.js`、`analysis.js`、`lib/tradeDate.js`
5. 前端空态引导：`decision.js` / `allocation.js` / `review.js` / `holdings.js`（2 处）+ `setup.js` 输出文案

### 阶段二已落地的部分

- 新建 `backend/lib/trackIndex.js`（唯一真相源，两仓 shared）：`TRACK_INDEX` 白名单、`INDEX_CODE_TO_TRACK` 映射、`FUND_TYPE_RULES`、`NAME_HINTS`、`needsTrackIndex`/`usesIndexAnchor`、`resolveTrackIndex`/`suggestLine`/`normalizeArchiveValue`/`listTrackIndexes`
- 实测单测通过（见第三节表格：10 只基金推断全部正确，含 `by=index` 覆盖与 `pending` 判定）

### 阶段二 · 已完成的接线清单

| 位置 | 落地内容 |
|---|---|
| `backend/lib/trackIndex.js` | **新建**（唯一真相源）：指数白名单 5 项 / `INDEX_CODE_TO_TRACK` / `FUND_TYPE_RULES` / `NAME_HINTS` / `needsTrackIndex` / `usesIndexAnchor` / `resolveTrackIndex` / `suggestLine` |
| `backend/fetchers.js` | 新增 `fetchFundArchive(code)`（含 **移动端 UA 修正**）+ 缓存 `data/cache/fund_archive_cache.json`（TTL 30 天）+ 新增 `fundAutoFill(code)` 合成「名单 + 档案 + 指数白名单 + 类别推断」 |
| `backend/lib/store.js` | `LAYOUT` 的 cache 段登记 `fund_archive_cache.json` |
| `backend/server.js` | 新增只读端点 `GET /api/track-index`；`/api/fund-lookup` 改用 `fundAutoFill`；类别校验由「启动时算一次的常量」改为 `allowedCategories()` **实时读盘**（5 秒 TTL），自建分类立刻生效 |
| `backend/engines/analysis.js` | ★ **F1 闸门**：`if (f.trackIndex && usesIndexAnchor(f.category))` —— 商品/债券/现金线永不启用指数锚；fund 对象补 `valuationAnchor:{hasTrackIndex,usable,degraded,reason}`；`byCat` 补漏网类别行 |
| `backend/engines/advice.js` | 未命中类别**不再 `continue`**，改为产出「待建设 / 未归类」显式卡片；正常卡片透出 `valuationAnchor` 并在 `conclusion` 前缀加降级警告 |
| `backend/engines/registry.js` | 三条线改名（见 3.1）；新增 `PENDING_CATEGORIES` / `isPendingCategory` |
| `backend/engines/alloc/allocation.js` | 未命中类别改为打 `_unsupported` 标记，`scoreMap` 也产出对应记录（否则前端连「待建设」都显示不出来） |
| `public/js/pages/holdings.js`（2026-09-23 拆分后为 `holdings/` 子目录：类别下拉在 `addFundPanel.js`、批量预览在 `bulkAdd.js`、`suggestCategory` 在 `fundMeta.js`） | 类别下拉选项源由 `engines` 改为 **`categories`**（否则用户选不到债券/现金）；新增**类别适用提示**；`suggestCategory` 兜底由 `'growth'` 改为 **`null`（不猜）**；自动带出优先用后端档案的 `trackIndex` / `suggestedCategory`；档案不可用时非阻塞降级 |
| `public/js/pages/decision.js` | 新增「缺估值锚·降级」badge + 说明行；待建设/未归类卡片不给综合分、只说明状态 |
| `public/js/api.js` | 新增 `getTrackIndex()` |
| `public/js/pages/allocation.js` | `BUCKET_COLORS` 增 `bond` / `cash` |
| `backend/scripts/verify_caliber_routing.js` | label 断言同步改名；夹具代码换成 `DEMO0x` 占位 |
| `backend/scripts/timing-smoke-test.js` | 夹具代码 `016664`（30 处）→ `DEMO01` |

**实测验收（全部通过）**

- 债基 `category=bond` → `/api/save` 返回 200；决策页显示「待建设 · 暂不判定」（综合分 null，不再静默消失）
- 无锚宽基（创业板指数基金）→ `valuationAnchor.degraded=true`，结论前缀带「⚠ 缺估值锚（跟踪指数）…不会给加仓信号」
- 环形图 6 块、**占比之和 = 100.00%**
- ★ **F1 回归**：给商品线基金挂上**合法**指数键 `SH000300`，`valuation.source` 仍为 `price`、`usable=false` —— 商品线未被污染
- `npm test` exit 0（9 组离线校验 + 脱敏审计 FAIL 0 / WARN 8 基线）；`npm run check:offline` 通过

### 阶段三 · 已完成

- `categories`（环形图线）从 4 条扩到 6 条：**+债券 +现金**，让它们独立成块，而不是落成「未归类（暂不支持）」
- `presets` / `customCategories` 两段新结构；「海外宽基」走 `category=broad + caliber=us` 组合预设，不新增 category 值
- `server.js` 的 `ALLOWED_CATEGORIES` **改为实时读盘**（现在启动时只读一次，自建分类无法生效）
- 三处静默点全部转可见：
  - `analysis.js` `byCat` → 按 `funds` 实际出现的 category 聚合，**保证占比和恒 =100%**
  - `advice.js` → 未命中不再 `continue`，改为产出 `{unsupported:true, unsupportedReason, verdict:'hold', score:null, detail:'该类别暂无决策算法，不参与买卖判定；市值仍计入总资产与配置占比。'}`
  - `allocation.js` → 未命中不再直接 `return`，`scoreMap` 也要有条目
- ✅ **配置页「类别管理」面板已实现**：列出内置预设（含「待建设」标记与适用说明）＋ 自建分类的增删。
  新增时填名字 + 选一条内置算法绑定，保存后 `store.reloadState()` 让新类别立刻出现在「添加基金」的下拉里。
  空看板时也会显示（那时恰恰最需要它）。

**自建分类的设计要点（三层配合，缺一层就断）**

| 层 | 做什么 | 为什么必须 |
|---|---|---|
| `categories`（展示线） | 新增时**同时**写入 `{key,name}` | 环形图分块按 `categories` 来，只写 `customCategories` 会落进「未归类」 |
| `customCategories` | 记录 `{key, name, category}`，`category` = 绑定的内置算法 | 后端 `okCategories` 校验它必须能挂到内置算法上 |
| `registry.baseCategoryOf()` | `resolveRegistry` 先把 `custom:xxx` **折算成内置算法 key** 再路由 | 自建分类是**别名不是新算法**，不折算就会被当成"没有算法"→ 该基金整只消失 |

附加修正：
- `analysis.js` 的 `usesIndexAnchor` / `needsTrackIndex` 也要传折算后的类别 —— 否则自建一个绑定到「宽基」的分类，会被当成"不能用估值锚"
- `isPendingCategory` 同样先折算 —— 用户自建「我的现金」绑定 `cash`，它同样是"待建设"，不能因 key 不是 `cash` 就漏判
- `server.js` 的 `allowedCategories()` 要登记自建分类的 **key**（用户实际存进 `fund.category` 的就是它），而不是它绑定的算法 key，否则保存会被 400 拦下

**实测验收**：自建「我的医药」绑定 `growth` → `/api/save` 200 → 加一只基金挂该分类 →
`categoryName` 显示「主题·行业（高波动）」（来自绑定的算法）、`unsupported=false`、综合分 10.1 正常出结论；
环形图里「我的医药」**独立成块**、占比之和 100.00%。

### 阶段五（📋 计划中，2026-09-24 定方案）：一键添加的「我的基金」页 + 每笔买入积分抵扣

> **本节是方案，尚未落地。** 行号以 2026-09-24 的分支状态为准，实施时需重新核对。

**目标**：把 §一 的产品契约再往前推一步。目前新用户加第一只基金要走「持仓 → 添加基金 → 填 6 个字段」，而其中**申购费、申购状态、单日上限其实都已经自动抓到了**，却要用户自己去查公告填限购 —— 这是面向陌生人最大的摩擦点。这一步把它们全部显式化，并把「加基金」简化成只填一个代码。

**顺带修一个已发现的漏判**：`backend/engines/alloc/allocation.js` 的暂停判定读的是**用户手填**的日限（`suspended = lim != null && lim <= 0`），抓来的 `sgState` 没有被消费 ⇒ 一只实际「暂停申购」的基金只要手填了非 0 的数，就会被判成「能买」，从而给出无法下单的加仓信号。

#### 5.1 页面归属：新增独立路由「我的基金」

| 位置 | 改动 |
|---|---|
| `public/js/pages/myfunds.js` | **新建**，只 export `render(container)`，与其它页同款 |
| `public/js/app.js` | 加 `import`；`PAGES` 加一行：label =「我的基金」、hash = `#/myfunds` |
| `public/js/pages/more.js` | ⚠️ `ITEMS` **必须补一条** —— 底部导航只有 概览/决策/持仓/更多 四项，其余页面全靠它兜底，**少这一处手机端点不到新页** |
| `public/sw.js` | `CACHE` 升版本（否则老用户看不到新页） |

- label 不与现有页冲突（`allocation` 占「配置」、`holdings` 占「持仓」）
- ⚠️ 新页若要复用 `pages/holdings/` 下的模块（它们是普通 ES 模块，可直接 import），**必须自己调 `state.js` 的 `setRoot()` + `setRerender()`** —— 这两个是模块级单例，不注册的话刷新会去重绘持仓页
- ★ **名字的代价靠文案补**：「我的基金」易与「持仓」混（持仓 = 已有份额与盈亏，本页 = 录入与基金档案），页面顶部必须把这层区分讲明

#### 5.2 内容：只问代码 + 两层信息显式分开

```
【基金档案】   名称 · 类别 · 跟踪指数            ← 自动带出，灰色只读
【费率与状态】 申购费 · 限大额 · 单日上限         ← 自动抓取，灰色只读
─────────── 以下是你自己的设置 ───────────
【我的每日上限】[____] 元（留空 = 不限）
【新买入默认】  [免申购费] [按申购费]
```

- **为什么必须视觉分离**：本项目最大的静默 bug 就是「申购费一直在参与算法，但界面没有这个概念的位置」。把抓取值与用户值摆成同一种样式，新用户会以为费率是他自己填的。
- 交易状态用徽章显示 `sgState`（暂停申购 / 限大额 / 开放申购）
- ★ `maxBuy` 的**「不限」是哨兵值**（1 后面 11 个 0），必须识别成「不限」，不能照原值显示；「暂停申购」时返回的极小值是占位 ⇒ **只显示状态、不显示上限**
- 一键添加复用已有的档案填充与批量粘贴；「我的每日上限」留空即不限，新用户不填也能跑

#### 5.3 每笔买入的「积分抵扣」标记

| 决策点 | 结论 |
|---|---|
| 粒度 | **每一笔买入**（积分是消耗品） |
| 抵扣后费率 | **全免 = 0** |
| 参与算法 | 勾上 → 该笔的**份额推导按 0 费率** |
| 控件 | 分段按钮（照 `formShell.js` 的 `sessionToggle` 范式写）。⚠️ **本项目禁止 `checkbox`** —— 有源码级断言钉住 |
| 默认值 | `true`（默认免申购费）。⚠️ 随之而来的三条责任见 5.7 |

字段：`feeWaived: true`，**只在 true 时写键**，缺失 ≡ 不免。不改 schema、不升版本、**不回填历史记录**。

★ **对已有记录零影响**：净投入对**已确认记录**（有份额 + 净值）取「份额 × 净值」券商真值，与费率无关 ⇒ 已确认记录**一个数字都不会变**，这个标记只是留痕。真正受影响的只有三处：① 预览份额；② 新落盘的份额；③ **在途记录**的净投入预估。

**后端改动**

| 位置 | 改动 |
|---|---|
| `backend/lib/buyPlan.js` | 新增 `effectiveFeeRate(waived, feeRate)`（true → 0，否则走既有的 `validFeeRate`）；`netInvestedOf` **只在在途分支**改用它（签名不变，调用点自动正确）；`previewOne` / `previewPurchase` 收 `waived` 并透传；导出追加 |
| `backend/engines/analysis.js` | 在途净投入的**重建对象**补 `feeWaived` —— 这是唯一会真丢字段的地方（重建时只挑了金额一个字段）。同段另一处传的是完整对象，不用改 |
| `backend/engines/backfill.js` | 解构加 `effectiveFeeRate` 并改用它（该文件头注释明文要求与 `buyPlan` **成对**，否则会出现「界面显示能算、实际永远不补」） |
| `backend/server.js` | 预览端点解析 `waived` 并透传；录入端点解析标记后算一个**生效费率**变量，**三处份额推导全部换用它**；**三处落盘显式写键**（见下方陷阱） |
| `pinFundFees` | **不用改**（只碰费率与费率明细，不碰买入记录） |

⚠️ **三个陷阱（实测定位）**：

1. 编辑记录时构造的 `upd` 是**全新对象**（不是 spread 旧对象）⇒ **不显式写 `feeWaived` 就会把标记抹掉**：编辑一笔改过日期的记录，标记静默消失。
2. 另一条路径（合并）是 `Object.assign({}, ex, {...})` ⇒ 旧的 `true` **盖不掉**。两条路径都必须显式赋值。
3. ★ 预览接口新增的参数名**不能含 `feeRate` 子串** —— 有一条负向断言要求「前端不得出现 `feeRate` 的赋值或字面量」，`&feeRate=` 会被直接命中。用 `waived` 即安全。

**前端改动**

| 位置 | 改动 |
|---|---|
| `holdings/constants.js` | 新增默认值常量（头注释的「导出：」行同步追加） |
| `holdings/formShell.js` | 新增 `feeToggle`（照既有 `sessionToggle` 范式）。⚠️ 必须 `addEventListener` —— 自研的 `el()` 对 `on*` 键只做 `k.slice(2)`，写 `onChange` 会绑成 `'Change'` 不触发 |
| `public/js/api.js` | `getPurchasePreview` 要加第 5 个参数 —— **这个函数是手工拼 query，不是透传，必须改** |
| `holdings/preview.js` | 解构取值 + 传参 |
| `holdings/purchaseForm.js` | 记一笔 / 编辑两个表单接入；payload 加标记；⚠️ `movedKey()` **不得含标记**（否则改标记会触发重算）；⚠️ payload 初始对象不得含 `recalc`（有断言） |
| `holdings/desktop.js`、`holdings/mobile.js` | 金额格加徽章（**不加列、不动 colspan**） |
| `holdings/bulkAdd.js` | 批量买入带上默认标记，且**只在为 true 时才写键**，避免给正常路径塞一堆 false |
| `public/style.css` | 加徽章样式（⚠️ 不得新增 `.chk` 类，有断言） |
| `public/sw.js` | `CACHE` 升版本 |

★★ **最高价值的坑：开关切换不会自动刷新预览。** 记一笔表单的预览只在日期变化、金额输入、交易时段切换三处被触发。新开关**必须把它的变更接到预览刷新** —— 否则界面上停留的是「不免」的份额，而落盘用的是「免」的份额，**用户看到的数字和存进去的不是一个**。这正是本项目最忌讳的静默不一致。

★ **编辑表单的开关初值必须取记录自身的标记**，不能取全局默认值 —— 默认值定为 true 后，初值一旦取全局默认，**编辑任意一笔老记录都会静默把标记盖到历史记录上**。

#### 5.4 ⚠️ 必须同时修改的既有断言（否则数据机门禁变红）

有几条断言目前假设「费率 > 0 时净投入必然小于实付」，全部豁免后会失效：

| 文件 | 改法 |
|---|---|
| `backend/scripts/verify_principal_caliber.js` | ① 顺序判据的豁免条件从「整只基金有一笔勾选就跳过」改为「**被收过费的钱 > 0**」—— 前者会**一票豁免整个基金**，等于把好几只基金的门禁一起关掉；② 期望申购费改为按**逐笔生效费率**累加 |
| `backend/scripts/verify_edit_recalc.js` | 夹具的逐字段断言里，份额期望值改用生效费率算 |

★ **这几条在本仓（空模板）跑不到** —— 空持仓会让相关分组 skip ⇒ 本仓 `npm test` **看不出红绿**，必须去数据机验一次。
★ 数据机上的私有夹具（`data/state/regression_cases.json` 的 `principalCaliber` / `editRecalcCases`）需在同步后人工确认，其中「费用明细之和 = 申购费总额」那条同样要改按逐笔生效费率算。

#### 5.5 新增断言

| 文件 | 断言 |
|---|---|
| `verify_principal_caliber.js` | 前端可写积分抵扣标记；`buyPlan` 导出生效费率函数；`server` 解析录入标记；`backfill` 与预览同口径；`analysis` 在途重建保留标记 |
| `verify_edit_recalc.js` | 两个表单都写标记进 payload |

> 「前端不得写 `feeRate`」与「无 checkbox」那几条**已存在，不要重复添加**。
> 两条方向相反的断言不打架：负向匹配小写 `feeRate` 紧跟 `:` 或 `=`，正向匹配 `feeWaived` 紧跟 `:`；新增函数名里的 `Rate` 是**大写 R**，不会命中负向正则。

#### 5.6 验证清单

**本仓能验**：

- `npm test`（FAIL 仍须 0、WARN 不高于 9 基线）
- **负向断言注入实测**（证明守卫真会红，而不是假绿）：把标记临时改成 `feeRate:` → 预期前端那条红；开关临时改成 `input type=checkbox` → 预期红；往入口壳注一行 → 预期闸门红
- 双视口手验（1440×900 与 390×844）：开关默认「免申购费」、切换后**份额预估立刻变**、徽章出现、手机端从「更多」能进新页

**只能去数据机验**：`npm test`（那里相关分组才会真跑）／同步后**记录条数与顶层结构零变化**／私有夹具是否需按新口径重算

**通用**：免申购费的份额 > 不免的份额，比值约等于「1 ÷ (1 − 费率)」（份额有舍入，不是精确相等）／已确认记录的份额与净值逐字段不变

#### 5.7 已定决策（2026-09-24 用户拍板）

1. **新页名 = 「我的基金」**（`#/myfunds`）。备选「基金库」未采用，代价与补偿见 5.1 末。
2. **积分抵扣默认值 = true（默认免申购费）** —— 按用户「每笔都用积分」的实际习惯定。**随之而来的三条责任**（必须一起做，否则这个默认值会咬人）：

   - 编辑表单的开关初值**必须取记录自身的标记**，不能取这个默认值
   - 开关文案要写清「默认已勾选：本笔用积分抵扣申购费」，让用户一眼知道自己在按 0 费率算
   - **开源前把这个常量重新讨论一次**，并在 README 的「数据来源与已知局限」里写明它的含义

3. **介绍页（landing）本轮不动** —— 用户方向仍是「功能设计完再做」，工作区现有的几个窄屏改动也不提交。

⚠️ **新增前端文件的三处登记**（少一处就是静默 bug）：`app.js` 的 `import` + `PAGES`、**`pages/more.js` 的 `ITEMS`**（最容易漏）、`sw.js` 的 `CACHE` 版本号。
⚠️ **新增 `pages/holdings/` 下模块时，头注释只能写纯中文职责描述** —— 那里的断言几乎全文扫描，标识符、像代码的行、硬编码数字都会踩红。

---

## 五、两仓同步（依据私有仓 `docs/版本更新流程.md`）

**手册 §0 原话：「代码改在『公开仓』，数据留在『数据机』。」** 顺序铁律：先 `--pull`，再 `--apply`；反了会静默丢东西且不报错。

```bat
rem  ① 公开仓改 + 自检 + 推
cd <公开仓>            && npm test && npm run check:offline && git add -A && git commit -m "feat: ..." && git push
rem  ② 回流数据机（否则数据机跑的还是旧代码）
cd <数据机>            && node backend/scripts/publish.js --check
                          node backend/scripts/publish.js --pull --public <公开仓>
```

### `publish.manifest.json` 四类

| 类别 | 本次涉及 |
|---|---|
| `shared`（两仓逐字节一致） | `backend/**/*.js`（含新增 `backend/lib/trackIndex.js`）、`public/**`、`data/example/*.example.json`、`data/config/theme_map.json`、`CHANGELOG.md`、4 篇 docs、`docs/img/**`、`.gitattributes`、`start.bat` |
| `publicOnly` | 现有 8 项 + **本次必须新增登记**：本文件 `用户开源版-设计改造文档.md`、`SECURITY.md`、`.github/ISSUE_TEMPLATE/*` |
| `privateOnly` | `backend/scripts/publish.js`、`data/state\|series\|cache/**`、`data/config/config.json`、`data/config/categories.json`、私有 docs、隐藏工具目录（该目录名本身即审计红线关键词，此处不写出）、`GITHUB_UPLOAD.md`、`publish.manifest.json` |
| `intentionalDivergence` | `.gitignore`、`package.json`、`package-lock.json` —— **不要顺手改** |

⚠️ **本文件（`用户开源版-设计改造文档.md`）是新增的仓库根目录文件，必须在 `publish.manifest.json` 里登记为 `publicOnly`**，否则 `publish.js --check` 的「② 未登记」会报出来。手册 §3 明确：⛔ 不要用通配符图省事。
⚠️ **给 `shared` 文件写文案时必须两边都成立**。例：`docs/后端架构清单.md` 里那句「仓库已转为私有」在数据机里是真的、在公开仓里是错的。本文件是 `publicOnly`，所以不受此限；但改 shared 文档时务必自查。
⚠️ `data/config/theme_map.json` 是 `shared`（**有意公开**），不要撤出公开仓。
⚠️ `data/config/categories.json` 是 `privateOnly` → 数据机需自行补 `presets`/`customCategories` 段，代码必须对两种结构都容错。

### 本机路径修正（手册里写的是另一台机器）

手册里的路径形如 `<用户目录>\pharos`（数据机）与 `<用户目录>\pharos-open`（工作台），那是**另一台机器**上的位置。本机的实际位置：

| 角色 | 本机实际路径 |
|---|---|
| 数据机（私有仓本地副本） | `X:\基金管理\Aurelius-fund-board-main\Aurelius-fund-board-main` |
| 工作台（公开仓本地副本） | `X:\pharos\pharos-open` |
| GitHub private | 私有仓。名称与地址**不登记在公开文档里**（带作者账号名会被脱敏审计判 `stale-url` WARN）——以本机 `git remote -v` 的实际输出为准 |
| GitHub public | `pharos-studio/pharos-open`（与手册一致） |

`publish.js` 默认对端仓 = 同级目录 `../pharos-open`，**在本机不存在** → 必须显式 `--public X:\pharos\pharos-open`。
git 命令在本机需**加沙箱豁免**，并使用系统 Git `C:\Program Files\Git\bin\git.exe`。

---

## 六、验收与回滚

### ★ 验收前置：必须清掉本地产物

`setup.js` 是**幂等**的（目标已存在即跳过、绝不覆盖）。本地若已有 `data/state/holdings.json`（含旧示例基金）与 `data/config/categories.json`（旧结构），则：

- 改空的 `holdings.example.json` **验不到** → 看到的还是旧基金
- 加 `presets` 的 `categories.example.json` **验不到** → 预设/自建分类不生效
- 且这些文件都在 `.gitignore` 里 → **`git status` 干净、不会报错，只会静默失真**

```bat
rem  清本地产物（都是 gitignored、可由 setup 重建）
rmdir /s /q data\state
del data\config\config.json
del data\config\categories.json
rem  ★ data\config\theme_map.json 是 git-tracked + shared，绝不能删
```
更稳的做法：**在全新 clone 里验首次运行**——那才是用户的真实路径。

### 验收清单

| 项 | 命令 / 判据 |
|---|---|
| 全量自检 | `npm test`（setup + 9 组离线校验 + 脱敏审计）**必须全绿** |
| 离线链纯度 | `npm run check:offline`（确认没混进联网脚本，CI 在境外会误报） |
| CI 环境复现 | `set TZ=UTC && npm test`（CI 跑在 UTC；⛔ 不要给 CI 加 TZ，它是时区哨兵） |
| 空看板 | 五页均不崩且显示引导文案；`/api/refresh` 返回全 0 且不抛异常 |
| 锚推断 | `resolveTrackIndex` / `suggestLine` 对第三节表格 10 只基金推断正确 |
| **F1 回归（重点）** | 给商品线基金挂上 `trackIndex` 后，**仍必须**走价格分位；`usesIndexAnchor('cycle') === false` |
| 待建设 | 加一只债基 → `/api/save` 200；决策页显示「待建设」卡；配置页环形图**各段之和 =100%** |
| 发布 | 私有仓 `publish.js --check` → **分叉 0 / 未登记 0 / 指纹命中 0** → `--pull` |

**审计模式**：空看板（`funds:[]` + DEMO 注释）→ **Mode B**（结构+关键词+内联 lint）。
数据机装上真实持仓后，`normalizeHoldings` 剥掉注释 → **Mode A** 指纹比对恢复。
⚠️ Mode B 的绿「≠ 脱敏安全」；真正的门禁在数据机 Mode A 与 `publish.js --apply` 的闸门 3。

### 回滚点

| 回滚对象 | 方法 |
|---|---|
| 本地产物 | 从 `X:\pharos\_verify_backup_20260919\` 还原 `state/`、`config/config.json`、`config/categories.json` |
| 代码改动 | 公开仓 `git revert <commit>`；数据机用 `publish.js --pull` 重新对齐 |
| schema 变更 | `normalizeHoldings` 只删 `_comment`、不碰结构，`_schemaVersion` 无需升版（`categories.json` 不归 schema 管辖） |

---

## 七、本轮不做（以及扩展点在哪）

- **债券 / 现金的真算法**。扩展点 = `backend/engines/registry.js` 的 `REGISTRY` 表 + `categories.json` 的 `engines`；看板先显示「待建设」
- **海外红利 / 标普红利的免费估值源**。`fetchers.js` 注释已定论：无免费源
- **把 category 从 4 值扩成 7 值的算法层重构**。扩展点 = `registry.js` 的复合键 `category:caliber`
- **自动把抓到的指数无提示写回持仓**。仍需用户在前端确认
- **`theme_map.json` 改为 privateOnly**（本次保持 `shared`）。但它的内容是清一色科技赛道，新用户穿透页会大面积「未分类」→ 引导用已有的「补词典向导」自行补充
- **`backtest_*.js` 这批研究脚本**（以及同类的 `make_checklist.js`）：仍以作者自己的基金代码作为功能常量（`FUND_CODE = '…'`），且 `make_checklist.js` 的输出落在 `docs/` 下的内部文档。这是目前**最大的剩余作者指纹**，建议后续决定：清理，或改为 `privateOnly` 并把输出路径挪出 `docs/`
- **`kernel.js` 的 `'008163'`**：这是 `yield_history.json` 旧平面结构的**数据迁移必需常量**，改了会破坏老数据兼容性，故保留

---

## 八、遗留风险

1. **`normalizeHoldings` 剥离注释后，本机 `npm run audit` 会切到 Mode A**（含 `lan-ip` 等本机专属检查），可能出现作者从未见过的失败。需观察，必要时把本机专属检查降为 WARN
2. **手册 §6 明令禁止「两边各改一半」**——所有改动一律在公开仓落地，再用 `--pull` 单向回流，不要手工挑文件复制
3. **阶段二接线前必须先落 F1 闸门**，否则自动填 `trackIndex` 会破坏商品线行为
4. 新缓存文件 `fund_archive_cache.json` 属 `data/cache/**` → `privateOnly`，不会外传，但需在 `store.js` 的 `LAYOUT` 登记
